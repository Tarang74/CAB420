%!TEX TS-program = xelatex
%!TEX options = -aux-directory=Debug -shell-escape -file-line-error -interaction=nonstopmode -halt-on-error -synctex=1 "%DOC%"
\documentclass{standalone}

\usepackage{unicode-math}
\setmathfont{Latin Modern Math}
\setmathfont{TeX Gyre Pagella Math}[range={bb,bbit}, Scale=MatchUppercase]

\usepackage{tikz}
\usepackage{pgfplots}

\usetikzlibrary{arrows.meta}

\definecolor{orange}{RGB}{255,215,18}
\definecolor{darkgrey}{RGB}{48,48,48}

\begin{document}
\begin{tikzpicture}[draw=darkgrey,x=2cm,y=2cm]
    % Axis
    \draw[->, thick, -{Stealth[round]}] (-1.5,0) -- (1.5,0) node[label={[label distance=1]135:\(z\)}] {};
    \draw[thick] (-1,-0.08) --++ (0,0.16);
    \node[label=below:\(y \equal -1\)] at (-1, 0) {};
    \begin{axis}[
        hide axis=true,
        at={(0,0)},
        anchor=origin,
        xmin=-1.25,
        xmax=1.25,
        ymin=0,
        ymax=1,
        clip=false,
        x=2cm,
        y=2cm,
    ]
        \addplot[color=orange,line width=2pt,opacity=0.9] table [x=z0, y=hinge0, col sep=comma] {tikz/losses/hinge.csv};
    \end{axis}
\end{tikzpicture}
\end{document}
